---
description: "Strict architectural rules and constraints for [DOMAIN NAME]"
globs: "[INSERT TARGET FOLDER GLOBS]"
alwaysApply: false
version: "1.0.0"
protocol_compat: "mcp: 2026-05"
dependencies: []
priority_schema: "critical > strong > guideline"
---

# Domain Execution Contract: [DOMAIN NAME]

<meta-instruction>
You have been routed here because the task modifies files matching this domain. Read XML tags as hard logic gates.
</meta-instruction>

## 1. Focused Best Practices

<positive-directives>
- You MUST [INSERT RULE].
- You MUST [INSERT RULE].
- You SHOULD [INSERT RULE].
</positive-directives>

## 2. Hard Domain Prohibitions

<absolute-constraints>
- DO NOT [INSERT STRICT BAN].
- DO NOT [INSERT STRICT BAN].
- DO NOT [INSERT STRICT BAN].
</absolute-constraints>

## 3. Context Gates

<conditional-logic>
IF [INSERT CONDITION]:
THEN [INSERT REQUIRED ACTION].
</conditional-logic>

## 4. Examples

<context>
<example>
// Good: [explain]
[insert compliant code]
</example>

<example>
// Bad: [explain]
[insert non-compliant code]
</example>
</context>

## 5. Verification

<pre-flight-checklist>
1. [ ] Did I comply with all absolute constraints?
2. [ ] Did I trigger any conditional logic?
3. [ ] Did I avoid ceremony without meaning?
</pre-flight-checklist>
